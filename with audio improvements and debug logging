[1mdiff --git a/src/components/GamePageOptimized.tsx b/src/components/GamePageOptimized.tsx[m
[1mindex 6249fe8..0769e60 100644[m
[1m--- a/src/components/GamePageOptimized.tsx[m
[1m+++ b/src/components/GamePageOptimized.tsx[m
[36m@@ -1,5 +1,6 @@[m
 import { useState, useEffect, useRef, useCallback, useMemo, memo } from "react";[m
 import { useNavigate } from "react-router-dom";[m
[32m+[m[32mimport { logGameState } from "../utils/gameDebug";[m
 [m
 // Memoize static data outside component to prevent recreation[m
 const BINGO_NUMBERS = Array.from({ length: 75 }, (_, i) => i + 1);[m
[36m@@ -93,73 +94,119 @@[m [mconst NumberButton = memo(({[m
   );[m
 });[m
 [m
[31m-// Lightweight audio manager - minimal preloading to reduce system lag[m
[32m+[m[32m// Simplified audio manager with better error handling[m
 class AudioManager {[m
   private cache = new Map<number, HTMLAudioElement>();[m
[32m+[m[32m  private failedFiles = new Set<number>();[m
 [m
   constructor() {[m
[31m-    // Minimal initialization[m
[32m+[m[32m    console.log('🔊 AudioManager initialized');[m
   }[m
 [m
[31m-  // Create audio on-demand to reduce memory usage and system lag[m
[32m+[m[32m  // Create audio element with simple, reliable approach[m
   private createAudio(num: number): HTMLAudioElement {[m
[31m-    let audio = this.cache.get(num);[m
[32m+[m[32m    // Skip cache entirely to avoid ERR_CACHE_OPERATION_NOT_SUPPORTED[m
[32m+[m[32m    // Create fresh audio element each time[m
     [m
[31m-    if (!audio) {[m
[31m-      audio = new Audio(`/sounds/${num}.wav`);[m
[31m-      audio.volume = 0.7;[m
[31m-      audio.preload = 'none'; // Don't preload to reduce system load[m
[31m-      this.cache.set(num, audio);[m
[31m-      [m
[31m-      // Limit cache size to prevent memory issues[m
[31m-      if (this.cache.size > 10) {[m
[31m-        const firstKey = this.cache.keys().next().value;[m
[31m-        if (firstKey !== undefined) {[m
[31m-          const oldAudio = this.cache.get(firstKey);[m
[31m-          if (oldAudio) {[m
[31m-            oldAudio.pause();[m
[31m-            oldAudio.src = '';[m
[31m-          }[m
[31m-          this.cache.delete(firstKey);[m
[31m-        }[m
[31m-      }[m
[31m-    }[m
[32m+[m[32m    const audio = new Audio();[m
[32m+[m[41m    [m
[32m+[m[32m    // Set basic properties[m
[32m+[m[32m    audio.volume = 0.7;[m
[32m+[m[32m    audio.preload = 'none';[m
[32m+[m[41m    [m
[32m+[m[32m    // Use aggressive cache busting to avoid browser cache issues[m
[32m+[m[32m    const timestamp = Date.now();[m
[32m+[m[32m    const random = Math.random().toString(36).substring(2, 15);[m
[32m+[m[32m    const audioSrc = `/sounds/${num}.wav?t=${timestamp}&r=${random}&nocache=true`;[m
[32m+[m[41m    [m
[32m+[m[32m    // Simple error handling[m
[32m+[m[32m    audio.addEventListener('error', () => {[m
[32m+[m[32m      console.warn(`❌ Audio load failed: ${num}.wav (cache bypass)`);[m
[32m+[m[32m      this.failedFiles.add(num);[m
[32m+[m[32m    }, { once: true });[m
[32m+[m[41m    [m
[32m+[m[32m    audio.addEventListener('loadstart', () => {[m
[32m+[m[32m      console.log(`🔊 Loading audio: ${num}.wav`);[m
[32m+[m[32m    }, { once: true });[m
[32m+[m[41m    [m
[32m+[m[32m    // Set src after event listeners are attached[m
[32m+[m[32m    audio.src = audioSrc;[m
     [m
     return audio;[m
   }[m
 [m
   playSound(number: number): void {[m
[32m+[m[32m    // Skip if we know this file failed before[m
[32m+[m[32m    if (this.failedFiles.has(number)) {[m
[32m+[m[32m      console.warn(`⏭️ Skipping ${number}.wav - known failed file`);[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m[41m    [m
     try {[m
       const audio = this.createAudio(number);[m
       [m
[31m-      // Reset and play[m
[31m-      audio.currentTime = 0;[m
[31m-      const playPromise = audio.play();[m
[31m-      [m
[31m-      if (playPromise) {[m
[31m-        playPromise.catch(() => {}); // Silent fail[m
[32m+[m[32m      // Simple play approach[m
[32m+[m[32m      if (audio.readyState >= 2) {[m
[32m+[m[32m        // Audio is ready, play immediately[m
[32m+[m[32m        audio.currentTime = 0;[m
[32m+[m[32m        audio.play().catch((error) => {[m
[32m+[m[32m          console.warn(`🔊 Play error for ${number}.wav:`, error.message);[m
[32m+[m[32m        });[m
[32m+[m[32m      } else {[m
[32m+[m[32m        // Audio needs to load first[m
[32m+[m[32m        const playWhenReady = () => {[m
[32m+[m[32m          audio.currentTime = 0;[m
[32m+[m[32m          audio.play().catch((error) => {[m
[32m+[m[32m            console.warn(`🔊 Delayed play error for ${number}.wav:`, error.message);[m
[32m+[m[32m          });[m
[32m+[m[32m        };[m
[32m+[m[41m        [m
[32m+[m[32m        if (audio.readyState === 0) {[m
[32m+[m[32m          // Not started loading yet[m
[32m+[m[32m          audio.addEventListener('canplay', playWhenReady, { once: true });[m
[32m+[m[32m          audio.load();[m
[32m+[m[32m        } else {[m
[32m+[m[32m          // Currently loading[m
[32m+[m[32m          audio.addEventListener('canplay', playWhenReady, { once: true });[m
[32m+[m[32m        }[m
       }[m
     } catch (error) {[m
[31m-      // Silent fail for better performance[m
[32m+[m[32m      console.warn(`🔊 Audio error for ${number}.wav:`, error);[m
[32m+[m[32m      this.failedFiles.add(number);[m
     }[m
   }[m
 [m
[31m-  cleanup(): void {[m
[31m-    this.cache.forEach(audio => {[m
[31m-      try {[m
[31m-        audio.pause();[m
[31m-        audio.src = '';[m
[31m-      } catch (error) {[m
[31m-        // Ignore cleanup errors[m
[31m-      }[m
[32m+[m
[32m+[m
[32m+[m[32m  // Debug method to check audio cache state[m
[32m+[m[32m  debugAudioCache(): void {[m
[32m+[m[32m    console.group('🔊 Audio Cache Debug');[m
[32m+[m[32m    console.log(`Cache size: ${this.cache.size}`);[m
[32m+[m[32m    console.log(`Failed files: ${Array.from(this.failedFiles)}`);[m
[32m+[m[41m    [m
[32m+[m[32m    this.cache.forEach((audio, num) => {[m
[32m+[m[32m      console.log(`Audio ${num}:`, {[m
[32m+[m[32m        src: audio.src,[m
[32m+[m[32m        readyState: audio.readyState,[m
[32m+[m[32m        networkState: audio.networkState,[m
[32m+[m[32m        error: audio.error?.message || 'none'[m
[32m+[m[32m      });[m
     });[m
[31m-    this.cache.clear();[m
[32m+[m[32m    console.groupEnd();[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  cleanup(): void {[m
[32m+[m[32m    // Since we're not using cache anymore, just clear the failed files list[m
[32m+[m[32m    this.failedFiles.clear();[m
[32m+[m[32m    console.log('🧹 Audio manager cleaned up');[m
   }[m
 }[m
 [m
 const GamePageOptimized = (): JSX.Element => {[m
   const navigate = useNavigate();[m
   const API_BASE_URL = import.meta.env.VITE_API_URL || '/api';[m
[32m+[m
[32m+[m
   [m
   // Core game state[m
   const [called, setCalled] = useState<number[]>([]);[m
[36m@@ -245,10 +292,11 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
         [m
         try {[m
           const gameData = JSON.parse(currentGame);[m
[31m-          if (!gameData.selectedCartelas || gameData.selectedCartelas.length === 0) {[m
[31m-            // Invalid game data, clear called numbers and redirect to NewGame[m
[31m-            console.log('Invalid game data found, clearing called numbers and redirecting to play bingo page');[m
[32m+[m[32m          if (!gameData.selectedCartelas || gameData.selectedCartelas.length < 3) {[m
[32m+[m[32m            // Invalid game data or insufficient cartelas, redirect to NewGame[m
[32m+[m[32m            console.log('Insufficient cartelas selected (need 3 minimum), redirecting to play bingo page');[m
             localStorage.removeItem('calledNumbers');[m
[32m+[m[32m            localStorage.removeItem('currentGame'); // Clear invalid game data[m
             setCalled([]);[m
             navigate('/newgame', { replace: true });[m
             return;[m
[36m@@ -313,6 +361,41 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
   // Game data[m
   const [currentGameData, setCurrentGameData] = useState<any>(null);[m
   [m
[32m+[m[32m  // Safety mechanism to reset isCallingNumber if it gets stuck[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    if (isCallingNumber) {[m
[32m+[m[32m      const resetTimer = setTimeout(() => {[m
[32m+[m[32m        console.warn('⚠️ Resetting stuck isCallingNumber state');[m
[32m+[m[32m        logGameState({[m
[32m+[m[32m          autoCall,[m
[32m+[m[32m          isCallingNumber,[m
[32m+[m[32m          isGameFinished,[m
[32m+[m[32m          selectedCartelas,[m
[32m+[m[32m          calledCount: called.length,[m
[32m+[m[32m          gameId: currentGameData?.id[m
[32m+[m[32m        });[m
[32m+[m[32m        setIsCallingNumber(false);[m
[32m+[m[32m        setAutoCall(false); // Also stop auto-call to prevent further issues[m
[32m+[m[32m      }, 15000); // Reset after 15 seconds if stuck[m
[32m+[m[41m      [m
[32m+[m[32m      return () => clearTimeout(resetTimer);[m
[32m+[m[32m    }[m
[32m+[m[32m  }, [isCallingNumber, autoCall, isGameFinished, selectedCartelas, called.length, currentGameData?.id]);[m
[32m+[m
[32m+[m[32m  // Debug logging for state changes[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    if (process.env.NODE_ENV === 'development') {[m
[32m+[m[32m      logGameState({[m
[32m+[m[32m        autoCall,[m
[32m+[m[32m        isCallingNumber,[m
[32m+[m[32m        isGameFinished,[m
[32m+[m[32m        selectedCartelas,[m
[32m+[m[32m        calledCount: called.length,[m
[32m+[m[32m        gameId: currentGameData?.id[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m[32m  }, [autoCall, isCallingNumber, isGameFinished]);[m
[32m+[m[41m  [m
   // Refs[m
   const autoCallIntervalRef = useRef<ReturnType<typeof setInterval> | null>(null);[m
   const audioManagerRef = useRef<AudioManager | null>(null);[m
[36m@@ -322,8 +405,35 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
   useEffect(() => {[m
     audioManagerRef.current = new AudioManager();[m
     [m
[32m+[m[32m    // Expose debug methods to window for debugging[m
[32m+[m[32m    if (process.env.NODE_ENV === 'development') {[m
[32m+[m[32m      (window as any).debugAudio = () => {[m
[32m+[m[32m        audioManagerRef.current?.debugAudioCache();[m
[32m+[m[32m      };[m
[32m+[m[41m      [m
[32m+[m[32m      (window as any).testAudio = (num: number) => {[m
[32m+[m[32m        console.log(`🧪 Testing audio ${num} with cache bypass...`);[m
[32m+[m[32m        audioManagerRef.current?.playSound(num);[m
[32m+[m[32m      };[m
[32m+[m[41m      [m
[32m+[m[32m      (window as any).testAudioMultiple = (nums: number[]) => {[m
[32m+[m[32m        console.log(`🧪 Testing multiple audio files with cache bypass...`);[m
[32m+[m[32m        nums.forEach((num, index) => {[m
[32m+[m[32m          setTimeout(() => {[m
[32m+[m[32m            console.log(`🔊 Playing ${num}...`);[m
[32m+[m[32m            audioManagerRef.current?.playSound(num);[m
[32m+[m[32m          }, index * 1000);[m
[32m+[m[32m        });[m
[32m+[m[32m      };[m
[32m+[m[32m    }[m
[32m+[m[41m    [m
     return () => {[m
       audioManagerRef.current?.cleanup();[m
[32m+[m[32m      if (process.env.NODE_ENV === 'development') {[m
[32m+[m[32m        delete (window as any).debugAudio;[m
[32m+[m[32m        delete (window as any).testAudio;[m
[32m+[m[32m        delete (window as any).testAudioMultiple;[m
[32m+[m[32m      }[m
     };[m
   }, []);[m
 [m
[36m@@ -367,27 +477,42 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
     };[m
   }, [selectedCartelas, isGameFinished, API_BASE_URL]);[m
 [m
[31m-  // Optimized auto-call[m
[32m+[m[32m  // Optimized auto-call with better error handling[m
   useEffect(() => {[m
[32m+[m[32m    // Always clear existing interval first[m
     if (autoCallIntervalRef.current) {[m
       clearInterval(autoCallIntervalRef.current);[m
       autoCallIntervalRef.current = null;[m
     }[m
 [m
[31m-    if (autoCall && !isGameFinished && selectedCartelas >= 3) {[m
[32m+[m[32m    if (autoCall && !isGameFinished && selectedCartelas >= 3 && !isCallingNumber) {[m
       autoCallIntervalRef.current = setInterval(async () => {[m
[31m-        if (document.hidden) return; // Don't call when tab is hidden[m
[32m+[m[32m        // Skip if document is hidden or already calling a number[m
[32m+[m[32m        if (document.hidden || isCallingNumber) return;[m
[32m+[m[41m        [m
[32m+[m[32m        // Set calling state to prevent concurrent calls[m
[32m+[m[32m        setIsCallingNumber(true);[m
         [m
         try {[m
           const token = localStorage.getItem('auth_token');[m
           const gameData = localStorage.getItem('currentGame');[m
           [m
[31m-          if (!token || !gameData) return;[m
[32m+[m[32m          if (!token || !gameData) {[m
[32m+[m[32m            setAutoCall(false);[m
[32m+[m[32m            return;[m
[32m+[m[32m          }[m
           [m
           const parsed = JSON.parse(gameData);[m
           const gameId = currentGameData?.id || parsed.gameId;[m
           [m
[31m-          if (!gameId) return;[m
[32m+[m[32m          if (!gameId) {[m
[32m+[m[32m            setAutoCall(false);[m
[32m+[m[32m            return;[m
[32m+[m[32m          }[m
[32m+[m
[32m+[m[32m          // Add timeout to prevent hanging requests[m
[32m+[m[32m          const controller = new AbortController();[m
[32m+[m[32m          const timeoutId = setTimeout(() => controller.abort(), 10000); // 10 second timeout[m
 [m
           const response = await fetch(`${API_BASE_URL}/games/${gameId}/call-number`, {[m
             method: 'PUT',[m
[36m@@ -395,9 +520,12 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
               'Authorization': `Bearer ${token}`,[m
               'Content-Type': 'application/json'[m
             },[m
[31m-            body: JSON.stringify({ calledNumbers: called })[m
[32m+[m[32m            body: JSON.stringify({ calledNumbers: called }),[m
[32m+[m[32m            signal: controller.signal[m
           });[m
 [m
[32m+[m[32m          clearTimeout(timeoutId);[m
[32m+[m
           if (response.ok) {[m
             const result = await response.json();[m
             [m
[36m@@ -419,10 +547,21 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
               audioManagerRef.current?.playSound(calledNumber);[m
             }[m
           } else if (response.status === 400 || response.status === 429) {[m
[32m+[m[32m            console.log('Auto-call stopped due to API response:', response.status);[m
             setAutoCall(false);[m
[32m+[m[32m          } else {[m
[32m+[m[32m            console.warn('Auto-call API error:', response.status);[m
[32m+[m[32m            // Don't stop auto-call for temporary server errors[m
           }[m
         } catch (error) {[m
[31m-          setAutoCall(false);[m
[32m+[m[32m          console.error('Auto-call error:', error);[m
[32m+[m[32m          if (error instanceof Error && error.name === 'AbortError') {[m
[32m+[m[32m            console.log('Auto-call request timed out');[m
[32m+[m[32m          }[m
[32m+[m[32m          // Don't stop auto-call for network errors, just log them[m
[32m+[m[32m        } finally {[m
[32m+[m[32m          // Always reset calling state[m
[32m+[m[32m          setIsCallingNumber(false);[m
         }[m
       }, slider * 1000);[m
     }[m
[36m@@ -433,7 +572,7 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
         autoCallIntervalRef.current = null;[m
       }[m
     };[m
[31m-  }, [autoCall, slider, isGameFinished, selectedCartelas, called, currentGameData, API_BASE_URL]);[m
[32m+[m[32m  }, [autoCall, slider, isGameFinished, selectedCartelas, called, currentGameData, API_BASE_URL, isCallingNumber]);[m
 [m
   // Memoized handlers[m
   const handleNumberClick = useCallback((_num: number) => {[m
[36m@@ -449,12 +588,22 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
       const token = localStorage.getItem('auth_token');[m
       const gameData = localStorage.getItem('currentGame');[m
       [m
[31m-      if (!gameData || !token) return;[m
[32m+[m[32m      if (!gameData || !token) {[m
[32m+[m[32m        console.error('Missing token or game data');[m
[32m+[m[32m        return;[m
[32m+[m[32m      }[m
       [m
       const parsed = JSON.parse(gameData);[m
       const gameId = currentGameData?.id || parsed.gameId;[m
       [m
[31m-      if (!gameId) return;[m
[32m+[m[32m      if (!gameId) {[m
[32m+[m[32m        console.error('Missing game ID');[m
[32m+[m[32m        return;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      // Add timeout to prevent hanging requests[m
[32m+[m[32m      const controller = new AbortController();[m
[32m+[m[32m      const timeoutId = setTimeout(() => controller.abort(), 10000); // 10 second timeout[m
 [m
       const response = await fetch(`${API_BASE_URL}/games/${gameId}/call-number`, {[m
         method: 'PUT',[m
[36m@@ -462,15 +611,18 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
           'Authorization': `Bearer ${token}`,[m
           'Content-Type': 'application/json'[m
         },[m
[31m-        body: JSON.stringify({ calledNumbers: called })[m
[32m+[m[32m        body: JSON.stringify({ calledNumbers: called }),[m
[32m+[m[32m        signal: controller.signal[m
       });[m
 [m
[32m+[m[32m      clearTimeout(timeoutId);[m
[32m+[m
       if (response.ok) {[m
         const result = await response.json();[m
         [m
         if (result.gameCompleted) {[m
           setAutoCall(false);[m
[31m-          setCalled(result.game.calledNumbers);[m
[32m+[m[32m          setCalled(result.game.calledNumbers || called);[m
           return;[m
         }[m
 [m
[36m@@ -486,9 +638,18 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
           // Play sound immediately[m
           audioManagerRef.current?.playSound(calledNumber);[m
         }[m
[32m+[m[32m      } else {[m
[32m+[m[32m        console.error('API error:', response.status);[m
[32m+[m[32m        if (response.status === 400) {[m
[32m+[m[32m          const errorData = await response.json();[m
[32m+[m[32m          console.error('Error details:', errorData);[m
[32m+[m[32m        }[m
       }[m
     } catch (error) {[m
[31m-      // Handle error[m
[32m+[m[32m      console.error('Manual next error:', error);[m
[32m+[m[32m      if (error instanceof Error && error.name === 'AbortError') {[m
[32m+[m[32m        console.log('Manual next request timed out');[m
[32m+[m[32m      }[m
     } finally {[m
       setIsCallingNumber(false);[m
     }[m
[36m@@ -1002,6 +1163,8 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
         </div>[m
       </div>[m
 [m
[32m+[m
[32m+[m
       <div style={{[m
         display: "flex",[m
         gap: "clamp(8px, 2vw, 20px)",[m
[36m@@ -1133,6 +1296,27 @@[m [mconst GamePageOptimized = (): JSX.Element => {[m
             >[m
               🔀 Shuffle[m
             </button>[m
[32m+[m[32m            {isCallingNumber && ([m
[32m+[m[32m              <button[m
[32m+[m[32m                style={{[m
[32m+[m[32m                  ...btnStyle,[m
[32m+[m[32m                  fontSize: "clamp(12px, 2.5vw, 16px)",[m
[32m+[m[32m                  padding: "clamp(8px, 2vw, 12px) clamp(12px, 3vw, 24px)",[m
[32m+[m[32m                  margin: 0,[m
[32m+[m[32m                  background: "linear-gradient(180deg, #DC2626 0%, #B91C1C 100%)",[m
[32m+[m[32m                  cursor: "pointer",[m
[32m+[m[32m                  whiteSpace: "nowrap" as const[m
[32m+[m[32m                }}[m
[32m+[m[32m                onClick={() => {[m
[32m+[m[32m                  console.log('🔄 Manual reset of calling state');[m
[32m+[m[32m                  setIsCallingNumber(false);[m
[32m+[m[32m                  setAutoCall(false);[m
[32m+[m[32m                }}[m
[32m+[m[32m                title="Reset if stuck calling numbers"[m
[32m+[m[32m              >[m
[32m+[m[32m                🔄 Reset[m
[32m+[m[32m              </button>[m
[32m+[m[32m            )}[m
           </>[m
         )}[m
       </div>[m
